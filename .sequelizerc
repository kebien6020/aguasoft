const { resolve } = require('path')

module.exports = {
  'migrations-path': resolve(__dirname, 'server/db/migrations'),
  'models-path': resolve(__dirname, 'server/db/models'),
  'config': resolve(__dirname, 'server/db/config.json'),
}
